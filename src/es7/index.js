//metodo include: nos va a permitir si hay un determinado elemento dentro de un valor 
let number = [1,2,3,4,5,6,7];
//1. Validar si dentro de este arreglo está el número 7

// arreglo.includes(valor a comprobar);
if (number.includes(7)) {
    console.log('El elemento si está');
} else {
    console.log('El elemento no está');
};


//elevar a la potencia 
let base = 4;
let exponente = 2;

console.log(base ** exponente);

